{
  "name": "homeio-mobile",
  "version": "0.1.0",
  "description": "Homeio mobile app — a Capacitor wrapper that connects to a self-hosted Homeio server over Tailscale.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "sync": "npm run build && cap sync && npm run apply:native",
    "add:android": "cap add android",
    "add:ios": "cap add ios",
    "run:android": "npm run sync && cap run android",
    "run:ios": "npm run sync && cap run ios",
    "lint": "tsc --noEmit",
    "apply:native": "node scripts/apply-native.mjs"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/app": "^7.0.0",
    "@capacitor/core": "^7.0.0",
    "@capacitor/ios": "^7.0.0",
    "@capacitor/preferences": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0",
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  }
}
